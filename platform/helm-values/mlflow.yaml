# MLflow community-charts/mlflow 1.8.5 values override
resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    memory: 2Gi
postgresql:
  enabled: false
service:
  type: ClusterIP
  port: 5000
