version: 0.2

# Publish the completed repack node of one KITScenes navigation recovery run.
# The coordinate is intentionally fixed so this launcher cannot mutate v2.2.
#
# Required start-build overrides:
#   FULL_RUN_EXECUTION_ID, IMAGE_TAG
# Optional:
#   DATASETS_BUCKET, COPY_WORKERS

env:
  shell: bash
  variables:
    AWS_DEFAULT_REGION: us-west-2
    FLYTE_PROJECT: auto-e2e
    FLYTE_DOMAIN: development
    FULL_RUN_EXECUTION_ID: ""
    PUBLISHED_DATASET: kitscenes
    DATASET_VERSION: v3.0
    DATASETS_BUCKET: ""
    DYNAMO_TABLE: auto-e2e-console
    COPY_WORKERS: "16"
    IMAGE_TAG: ""

phases:
  install:
    commands:
      - pip install -q flytekit==1.14.9 numpy torch kubernetes --extra-index-url https://download.pytorch.org/whl/cpu
  build:
    commands:
      - ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      - ECR_URL="${ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"
      - test -n "${FULL_RUN_EXECUTION_ID}"
      - test -n "${IMAGE_TAG}"
      - test "${PUBLISHED_DATASET}" = "kitscenes"
      - test "${DATASET_VERSION}" = "v3.0"
      - |
        if [ -z "${DATASETS_BUCKET}" ]; then
          DATASETS_BUCKET="auto-e2e-platform-datasets-${ACCOUNT_ID}"
        fi
        export DATASETS_BUCKET
      - |
        image_ref() {
          REPOSITORY="$1"
          DIGEST=$(aws ecr batch-get-image \
            --repository-name "auto-e2e/${REPOSITORY}" \
            --image-ids imageTag="${IMAGE_TAG}" \
            --query 'images[0].imageId.imageDigest' \
            --output text)
          if [ -z "${DIGEST}" ] || [ "${DIGEST}" = "None" ]; then
            echo "No digest found for auto-e2e/${REPOSITORY}:${IMAGE_TAG}" >&2
            return 1
          fi
          printf '%s/auto-e2e/%s@%s' \
            "${ECR_URL}" "${REPOSITORY}" "${DIGEST}"
        }
        AUTO_E2E_TRAINING_IMAGE="$(image_ref training)" || exit 1
        AUTO_E2E_EVAL_IMAGE="$(image_ref eval)" || exit 1
        AUTO_E2E_OFFLINE_RL_IMAGE="$(image_ref offline-rl)" || exit 1
        AUTO_E2E_DATA_PREP_IMAGE="$(image_ref data-prep)" || exit 1
        export AUTO_E2E_TRAINING_IMAGE AUTO_E2E_EVAL_IMAGE
        export AUTO_E2E_OFFLINE_RL_IMAGE AUTO_E2E_DATA_PREP_IMAGE
        export ECR_PREFIX="${ECR_URL}"
        export PYTHONPATH="${CODEBUILD_SRC_DIR}/Model:${CODEBUILD_SRC_DIR}:${PYTHONPATH:-}"
      - |
        cat > /tmp/flyte.yaml << EOF
        admin:
          endpoint: dns:///k8s-flyte-flyteadm-be92e7737c-cf0d1a0f3df10f77.elb.us-west-2.amazonaws.com:81
          insecure: true
        EOF
      - |
        python3 Platform/scripts/extract_full_run_overlay_inputs.py \
          --config /tmp/flyte.yaml \
          --project "${FLYTE_PROJECT}" \
          --domain "${FLYTE_DOMAIN}" \
          --execution-id "${FULL_RUN_EXECUTION_ID}" \
          --expected-dataset "KIT-MRT/KITScenes-Multimodal" \
          --expected-dataset-version "${DATASET_VERSION}" \
          --allow-running-recovery \
          --output /tmp/recovery-inputs.json
      - |
        python3 - <<'PY'
        import json

        with open("/tmp/recovery-inputs.json", encoding="utf-8") as stream:
            recovered = json.load(stream)
        with open("/tmp/publication-inputs.json", "w", encoding="utf-8") as stream:
            json.dump({"shards": recovered["shards"]}, stream)
        PY
      - |
        pyflyte --config /tmp/flyte.yaml run --remote \
          --project "${FLYTE_PROJECT}" \
          --domain "${FLYTE_DOMAIN}" \
          --image "${AUTO_E2E_TRAINING_IMAGE}" \
          Platform/pipelines/workflows.py \
          wf_publish_dataset_snapshot \
          --inputs-file /tmp/publication-inputs.json \
          --published_dataset "${PUBLISHED_DATASET}" \
          --datasets_bucket "${DATASETS_BUCKET}" \
          --dataset_version "${DATASET_VERSION}" \
          --dynamo_table "${DYNAMO_TABLE}" \
          --aws_region "${AWS_DEFAULT_REGION}" \
          --copy_workers "${COPY_WORKERS}"
